text/html; xdg-open %s; test=test -n "$DISPLAY"; needsterminal;
text/html; w3m -I %{charset} -T text/html; copiousoutput;
application/*; xdg-open %s; test=test -n "$DISPLAY"; copiousoutput;
image/*; xdg-open %s; test=test -n "$DISPLAY"; copiousoutput;

